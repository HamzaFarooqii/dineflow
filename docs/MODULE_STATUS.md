# Module Status

Living document — update at the end of every development day (`RULES.md` §Daily Completion).
Reflects `develop` after a post-Day-4 gap-filling pass (Hamza, ahead of Day 5): unit conversion in
recipes, ingredient edit/deactivate/reactivate, real staff roles beyond cashier/manager, clock-in/
out shifts, a configurable service charge, and an equal-split-bill calculator. Days 1–4 (see
`docs/day-plans/day2.md`/`day3.md`/`day4.md`) are unchanged and still fully complete. Day 5 itself
has not started — see `docs/day-plans/day5-ahmed.md` and `docs/day-plans/day5-bisma.md` for the
work division (includes two real gaps this pass found: `FloorScreen`/`KitchenScreen` have no
terminal mode yet, so the new waiter/chef roles have nowhere to go on an actual terminal until
those screens gain one). Status legend:
✅ Complete · 🟡 Partial · 🔴 Missing · 🔵 Needs redesign · 🟠 Needs refactor · ⚠️ Buggy/risky · ♻️ Reusable as-is

| Module | Status | UI | Backend | DB | Tests | Action |
|---|---|---|---|---|---|---|
| **A. POS / Register** | 🟡 | Order-type pills, menu grid, cart, discounts ✅. Guest selection mandatory, stock-oversell warning on cart lines ✅. A cart line's discount popover now offers Reward/Promo redemption alongside manual percent/fixed ✅ (Day 4). **New:** a configurable service charge (Settings → Restaurant details) shown on the register and payment screens ✅; an equal-N-way split-bill calculator on the payment screen ✅ (UI-only — still recorded as one payment, see Action). Modifiers 🔴 (visible placeholder only) | Checkout, discount/manager-approval, refunds ✅. order_type/table_id persist ✅. Kitchen tickets fire straight to `preparing` ✅. Loyalty award/redeem now runs in the same transaction as order creation ✅ (Day 4, row I). **New:** service charge is snapshotted per sale (`service_charge_bps`/`service_charge_cents`) the same way each line snapshots its own `tax_bps`, and `orders.ts` re-derives and rejects a mismatch exactly like every other total ✅ | `pos_orders`(+order_type,table_id,service_charge_cents), `pos_order_items`, `pos_payments`, `stores`(+service_charge_bps) ✅ | Strong (checkout, discount, order-sync, duplicate-submit, employee-attribution, `orders-loyalty.test.ts`, new service-charge cases in `money.test.ts`/`checkout.test.ts`/`orders.test.ts`). Oversell/mandatory-guest/auto-fire changes verified via existing suite + manual QA, no new automated test (see day3.md's honest note on `push()`'s testability) | Modifiers and hold-resume: not yet scheduled, see `docs/day-plans/day5-ahmed.md` item 2 for modifiers. Split bill is deliberately UI-only, not itemized/per-seat — `pos_payments` has a real `unique(store_id, order_id)` constraint several other features depend on, and relaxing it is a larger structural change out of this pass's scope. **Fixed (post-Day-4):** the web (non-terminal) register's discount checkout previously hit the cashier-terminal's 20% manager-approval cap with no modal to clear it (owner accounts included) since `ManagerApprovalModal` only ever renders on the terminal path — `completeLocalSale` now takes a `terminal` flag and only enforces that cap when it's true, so the web register (always an owner/manager session) is no longer blocked. See day4.md's "Post-Day-4 fix" note |
| **B. Front of House / Tables** | 🟡 | Floor screen, area tabs, table cards (showing last order total), Seat/Add order/Bill/Settle/Clean ✅. Full area/table create/edit/delete behind an "Edit floor" toggle ✅. Transfer/Merge ✅ (real implementation, not the old disabled placeholder). **Gap found this pass:** `FloorScreen` has no terminal mode — a cashier terminal (including the new `waiter` role) cannot open the Floor screen at all yet, even though the API already supports it (`terminal` param already threaded through `fetchFloorPlan`) | `/floor` read + atomic status-transition PATCH ✅. `applyTableStatusTransition` shared with the kitchen's served-ticket hook. Area/table CRUD endpoints + `moveTableParty` (Transfer/Merge, transaction-locked, deadlock-safe) ✅ | `floor_areas`, `restaurant_tables`(+assigned_waiter_id) ✅ | `floor.test.ts` (PGlite): the status-transition primitive, manager-vs-terminal authorization, and `moveTableParty`'s transfer/merge/precondition-rejection behavior (6 tests total) ✅ | Reservations/waitlist not scheduled. "Last order total" is honestly a completed order, not a live running tab — no open-ticket layer exists to source one from (see floor.ts's comment). Terminal mode for `FloorScreen` assigned to Bisma, `docs/day-plans/day5-bisma.md` item 2 |
| **C. Menu** | 🟡 | Category tabs, search, availability badge ✅ (register + back-of-house). Recipe builder + food-cost % in the product editor ✅. Variants/sizes/add-ons/combos 🔴 | Catalog snapshot includes restaurant columns ✅. `/catalog` recipe + unit endpoints ✅ | `pos_products`(+station_id, course, kitchen_name, is_available, unavailable_until, prep_time_seconds) ✅. No `description`/variant columns | Existing catalog tests pass; recipe/unit endpoints covered (catalog.test.ts) | Real modifier groups (e.g. required single-select "Size", optional multi-select "Add-ons", each option with its own price delta) scoped and assigned to Ahmed, `docs/day-plans/day5-ahmed.md` item 2, including a suggested schema — not yet started |
| **D. Kitchen / KDS** | 🟡 | KDS board, Mark ready/Serve ✅ ("Fire" is now rarely seen — see Backend). **Gap found this pass:** `KitchenScreen` has no terminal mode — a cashier terminal (including the new `chef` role) cannot open the KDS board at all yet, even though the API already supports it (`terminal` param already threaded through `fetchKitchenTickets`) | Ticket creation on every order, station grouping, item transitions ✅. New tickets fire straight to `preparing` instead of `queued` ✅. A fully-served dine-in ticket flips its table to `served` (calls `floor.ts`'s shared transition primitive directly, best-effort — see `kitchen.ts`'s `patchItem`); a manager can also trigger it by hand from the Floor screen's "Mark served" button, a cashier terminal cannot. Serving an item now also decrements its recipe's ingredient stock via `stock_movements` (`consumeRecipeIngredients`) — allowed to go negative rather than blocking the serve, matching `pos_stock`'s existing oversell reasoning, surfaced via a new "Out of stock" tag on the Inventory screen. **New:** consumption now converts across units (see row E) instead of skipping a line whose unit merely differs from the ingredient's, using the same `convertQuantity` function costing uses | `kitchen_tickets`, `kitchen_ticket_items` ✅ | orders.test.ts + kitchen-ticket-status.test.ts + floor.test.ts (table-sync paths) + kitchen.test.ts (6 PGlite tests: consumption math, negative-stock behavior, idempotency, unit-mismatch skip, unit-conversion, no-recipe no-op) ✅. The auto-fire change itself has no new automated test — `push()` is DB+auth-coupled with no existing integration harness, verified manually instead (flagged honestly, not hidden) | Delay/SLA tracking and course-based firing not built. Standalone "Ticket CRUD" deliberately not built — a ticket is derived 1:1 from a paid order; that invariant matters more than the feature. Terminal mode for `KitchenScreen` assigned to Ahmed, `docs/day-plans/day5-ahmed.md` item 3 |
| **E. Recipes** | ✅ | Recipe builder UI in the product editor (add-dish drawer + a drawer for existing dishes), costed ingredient lines, estimated food-cost % ✅. Ingredient picker is a search-as-you-type combobox with inline "+ Create new ingredient…" (name, unit, cost per unit) via `IngredientSelector.tsx` ✅. **New:** units can carry an optional `factor_to_base` conversion factor (curated common units like mg/g/kg/ml/L ship correct factors automatically; a custom unit can optionally set one) — a recipe line in grams now costs correctly against an ingredient stocked in kilograms, instead of only costing when the units match exactly ✅ | `/catalog` recipe + unit endpoints, full replace-on-save ✅. `saveRecipe` validates convertibility (not exact match) via the same `convertQuantity` function costing and consumption both use | `units`(+factor_to_base), `recipes` ✅ | 8 domain tests (costing + conversion math), 6 API tests, 6 web tests ✅ | Two units of the same kind with no factor set on either stay unconvertible on purpose (never guessed at as 1:1) — the identical unit always still costs directly regardless |
| **F. Restaurant Inventory** | ✅ | Finished-item stock/oversold reporting ♻️ reusable as-is. Ingredient-level: full `/inventory` screen (list, low-stock + out-of-stock tags, batches with expiry tones, wastage form, movement ledger) ✅, plus cashier-terminal writes gated behind manager-PIN approval ✅. **New:** Edit and Deactivate/Reactivate controls on the ingredient detail header ✅ — a deactivated ingredient stays findable under a new "Inactive" filter (with a muted status badge) rather than disappearing, so it can actually be reactivated later | `pos_stock` (finished items) ✅; `ingredients`/`ingredient_batches`/`stock_movements` CRUD + wastage + consumption-wiring ✅. **New:** `PATCH /ingredients/:id/reactivate` (mirrors the existing deactivate endpoint) | `pos_stock` ✅; `ingredients`, `ingredient_batches`, `stock_movements`, `recipe_ingredients` ✅ | loadOversold covered; inventory.test.ts (API, wastage-boundary) + inventory.test.ts (web, low-stock/out-of-stock/expiry-tone, new inactive-filter test) + kitchen.test.ts (consumption wiring) ✅. `reactivateIngredient`'s HTTP handler itself is manual-QA-only, same honest carve-out as `deactivateIngredient` already had | One known non-blocking gap remains: `ingredients`/`stock_movements.manager_id` uses a bare FK instead of this codebase's composite `(store_id, id)` tenant-scoping convention |
| **G. Purchasing & Vendors** | 🔴 | none | none | none | none | Beyond the 5-day sprint per original scope note — do not start until Days 3–5 core modules land |
| **H. Customers / CRM** | 🟡 | Guest directory, search (phone or name, one field) ✅, create ✅. Guest profile dialog ("View profile") shows lifetime spend + recent visits ✅, aggregated live from `pos_orders` (no duplicate running-total column). Points balance + tier shown via `<LoyaltyBalance>` on every guest card (directory and the register's guest picker), with an explicit "Enroll in loyalty" action for guests who aren't enrolled ✅. Favorites: 🔴 | Customer CRUD, search (phone or name) ✅. `GET /customers/:id/summary` (+ `/pos/customers/:id/summary`) aggregation endpoint ✅ | `pos_customers` ✅ | customer-api, customer-migration, `customers.test.ts` (lifetime-spend/visit-count aggregation, store+guest scoping, zero-orders case) ✅ | Favorites not built. The tier/balance display now goes through Ahmed's `/loyalty` API (row I) end to end, not a direct-Supabase read — the two independently-built implementations were consolidated onto his during the Day 4 merge |
| **I. Loyalty** | ✅ | Points balance + tier shown on every guest card (Guests screen + register guest picker) via `<LoyaltyBalance>`, with explicit opt-in enrollment, sync/offline-aware states, and a Retry action ✅. Reward rules (create/edit/deactivate/reactivate) manageable from the Guests screen, owner/manager only ✅. A cart line's discount popover now has a "Reward" tab (register, both web and terminal) that lists rewards the guest's balance can afford and applies one as an ordinary line discount ✅. No tier-configuration UI yet — tiers can only be read, not created, so every account shows "No tier" until one is inserted by hand | `/loyalty` (web) and `/pos/loyalty` (terminal): accounts, opt-in enrollment, paginated ledger, tiers (read), reward-rules CRUD ✅, all tenant-scoped. **Checkout wiring ✅**: `orders.ts`'s `push()` awards points (tier-multiplied, based on lifetime points before the order) and deducts a redeemed reward's cost, both in the same transaction as the order itself — idempotent via the existing `operation_id` replay guard, clamped (never sale-blocking) if a queued offline sale's redemption balance turns out stale by the time it syncs | `loyalty_tiers`, `loyalty_accounts`, `loyalty_point_ledger`, `reward_rules` ✅ (Hamza's Day 4 schema, applied and recorded in `APPLIED.md`) | 4 schema-integrity tests + 9 domain tests (`loyalty.test.ts`) + full API test suite (every endpoint, web and terminal paths, cross-store/cross-role rejection) + a web form-draft test + `orders-loyalty.test.ts` (9 subtests: base-rate/tier-rate earning, redeem-and-earn together, clamped redemption, idempotent retry, redemption without a customer rejected) | Nobody can configure a tier yet — assigned to Ahmed, `docs/day-plans/day5-ahmed.md` item 1, same CRUD pattern as reward rules already has |
| **J. Promotions** | ✅ | Owner/manager-only `/promotions` screen ✅ (create/edit/deactivate, `starts_at`/`ends_at` window, active toggle), routed and in the Manage nav. A cart line's discount popover now has a "Promo" tab listing currently-active promotions and applying one as an ordinary line discount ✅ | `promotionsRouter` (`GET`/`POST`/`PATCH /promotions`), manager-only, in `apps/api/src/app.ts` ✅. **New**: `GET /pos/promotions` (terminal-only, read-only, active-and-in-window filtered) — a gap the checkout-wiring task found: the management endpoints are manager-only by design, but nothing let a cashier terminal *read* promotions to apply one until this | `public.promotions` ✅ (`202609250003_promotions.sql`, applied and recorded in `APPLIED.md`) — `discount_kind`/`discount_value` mirror `LineDiscount`'s `{percent,bps}\|{fixed,cents}` shape for a direct mapping | `promotions.test.ts` (domain: window/active filtering, discount mapping, 8 tests) + `promotions.test.ts` (API/PGlite: constraint checks, active toggle) + `orders-loyalty.test.ts`'s `/pos/promotions` subtest (active/in-window filter, store isolation) ✅ | Applying a promotion doesn't currently check it isn't double-stacked with a reward or a manual discount on the same line — a line still only carries one `LineDiscount`, so this is a UI ordering question (whichever's applied last wins), not a data-integrity gap |
| **K. Staff** | 🟡 | Owner/Manager (web) + Cashier/Manager/**Waiter/Chef/Inventory Manager/Rider** (terminal) roles ✅, employee management (Settings→Team, role dropdown now lists all six) ✅. **New:** clock-in/out — a `ClockButton` in the cashier topbar, visible on every `/pos/*` screen ✅. Tips: 🔴 | terminal-auth (PIN, sessions, approval) ✅. **New:** `packages/domain/src/staff-role.ts`'s capability matrix (`register`/`floor`/`kitchen`/`inventory`/`staff`) drives `CashierPosLayout`'s nav visibility per role — client-side only, not a new server-side enforcement layer (existing manager-PIN-evidence checks on inventory/discount writes are unaffected and still the real authority boundary). `POST /pos/shifts/clock-in`/`clock-out`, `GET /pos/shifts/current`, `GET /shifts` (owner/manager) ✅ | `store_memberships`, `terminal_employees`(role constraint widened to 6 roles), **new `shifts` table** (one open shift per employee, enforced by a partial unique index) ✅ | terminal-auth security tests ✅ (strong — salted PIN hashing, rollback/expiry). `staff-role.test.ts` (capability matrix, 4 tests) + `shifts.test.ts` (PGlite/HTTP: clock-in/out lifecycle, double clock-in rejected, cross-store rejection, 8 subtests) ✅ | Waiter and Chef roles exist and can log in, but have nowhere to go yet — `FloorScreen`/`KitchenScreen` have no terminal mode (see rows B and D; assigned to Bisma and Ahmed respectively). Rider has no capability or screen at all yet — no delivery-tracking feature exists in this codebase to attach it to; revisit when one does. Shifts is deliberately minimal (no breaks, no payroll export) |
| **L. Reports** | 🟡 | Owner/cashier dashboards, daily summary, order history, oversold ✅. Dish profitability/food-cost/kitchen-performance/wastage/loyalty/hours-worked reports: 🔴 | loadDailySummary/loadOrdersPage/loadOversold ✅, timezone-correct. `GET /shifts` now exists as raw data for an hours-worked report to aggregate | reads existing tables | reports.test.ts (DST-aware, PGlite-backed) ✅ | Day 5 — not started. Ahmed: dish profitability/food-cost/kitchen-performance (`docs/day-plans/day5-ahmed.md` item 4). Bisma: owner dashboard, customer/loyalty, inventory, hours-worked (`docs/day-plans/day5-bisma.md` items 1 and 3). Kitchen-performance data already exists (`kitchen_tickets` timestamps), just unsurfaced |
| **M. Restaurant Intelligence** | 🔴 | none | none | none | none | Correctly not started — no real operational data volume yet to forecast from. Revisit after Day 5 |

## Cross-cutting infrastructure (not a "module" but load-bearing)

| Area | Status | Note |
|---|---|---|
| Auth | ✅ | Two systems by design: Supabase Auth (owner/manager, web) + PIN-based terminal auth (cashier, offline-capable). Don't merge them. |
| RLS / tenant isolation | ✅ | Consistent member-read pattern across every restaurant table added so far; writes go through the API (service-role) only. |
| Offline/sync | ✅ | Dexie outbox pattern, well-tested (order-sync-core, duplicate-submit). Don't introduce a second sync mechanism for new modules — extend this one. |
| Design system | ✅ | MISE tokens (`styles.css` Section E), applied everywhere. See `docs/DESIGN_SYSTEM.md`. |
| CI | ✅ | GitHub Actions, build+test on every PR/push to `develop`/`main`. |
| Testing | 🟡 | Strong unit/integration coverage on money math, checkout, sync, terminal-auth. No component-level or E2E/browser test runs in CI (browser-check scripts exist but are manual-only). |
| Performance | ⚠️ | Single JS bundle is 800KB+ (Vite's own build warning) — no code-splitting beyond three lazy-loaded routes. Flagged for Day 5 hardening, not urgent before then. |
| Migration ledger discipline | 🟠 | `APPLIED.md` process works but has already needed one Lead fixup (Day 2) for a missed entry and a numbering collision between two same-day migrations. `RULES.md` tightens this. |
