# Module Status

Living document — update at the end of every development day (`RULES.md` §Daily Completion).
Reflects `develop` as of Day 4's completion (`docs/day-plans/day4.md` has the full detail):
Day 3's work (Ahmed's recipes/costing, Bisma's ingredient inventory, Hamza's checkout-integrity
fixes/floor CRUD/Transfer/Merge/consumption-wiring) plus all of Day 4's (Hamza's loyalty schema,
product-wide design-system redesign and loyalty/promotions checkout wiring; Ahmed's loyalty
module; Bisma's promotions + guest CRM) are merged. Day 4 is fully complete — see row I and J for
the checkout-wiring detail. Status legend:
✅ Complete · 🟡 Partial · 🔴 Missing · 🔵 Needs redesign · 🟠 Needs refactor · ⚠️ Buggy/risky · ♻️ Reusable as-is

| Module | Status | UI | Backend | DB | Tests | Action |
|---|---|---|---|---|---|---|
| **A. POS / Register** | 🟡 | Order-type pills, menu grid, cart, discounts ✅. Guest selection mandatory, stock-oversell warning on cart lines ✅. A cart line's discount popover now offers Reward/Promo redemption alongside manual percent/fixed ✅ (Day 4). Modifiers/split bill/service charge 🔴 (visible placeholder only) | Checkout, discount/manager-approval, refunds ✅. order_type/table_id persist ✅. Kitchen tickets fire straight to `preparing` ✅. Loyalty award/redeem now runs in the same transaction as order creation ✅ (Day 4, row I) | `pos_orders`(+order_type,table_id), `pos_order_items`, `pos_payments` ✅ | Strong (checkout, discount, order-sync, duplicate-submit, employee-attribution, `orders-loyalty.test.ts`). Oversell/mandatory-guest/auto-fire changes verified via existing suite + manual QA, no new automated test (see day3.md's honest note on `push()`'s testability) | Split bill/service charge/hold-resume: not yet scheduled — flag if needed. **Fixed (post-Day-4):** the web (non-terminal) register's discount checkout previously hit the cashier-terminal's 20% manager-approval cap with no modal to clear it (owner accounts included) since `ManagerApprovalModal` only ever renders on the terminal path — `completeLocalSale` now takes a `terminal` flag and only enforces that cap when it's true, so the web register (always an owner/manager session) is no longer blocked. See day4.md's "Post-Day-4 fix" note |
| **B. Front of House / Tables** | ✅ | Floor screen, area tabs, table cards (showing last order total), Seat/Add order/Bill/Settle/Clean ✅. Full area/table create/edit/delete behind an "Edit floor" toggle ✅. Transfer/Merge ✅ (real implementation, not the old disabled placeholder) | `/floor` read + atomic status-transition PATCH ✅. `applyTableStatusTransition` shared with the kitchen's served-ticket hook. Area/table CRUD endpoints + `moveTableParty` (Transfer/Merge, transaction-locked, deadlock-safe) ✅ | `floor_areas`, `restaurant_tables`(+assigned_waiter_id) ✅ | `floor.test.ts` (PGlite): the status-transition primitive, manager-vs-terminal authorization, and `moveTableParty`'s transfer/merge/precondition-rejection behavior (6 tests total) ✅ | Reservations/waitlist not scheduled. "Last order total" is honestly a completed order, not a live running tab — no open-ticket layer exists to source one from (see floor.ts's comment) |
| **C. Menu** | 🟡 | Category tabs, search, availability badge ✅ (register + back-of-house). Recipe builder + food-cost % in the product editor ✅. Variants/sizes/add-ons/combos 🔴 | Catalog snapshot includes restaurant columns ✅. `/catalog` recipe + unit endpoints ✅ | `pos_products`(+station_id, course, kitchen_name, is_available, unavailable_until, prep_time_seconds) ✅. No `description`/variant columns | Existing catalog tests pass; recipe/unit endpoints covered (catalog.test.ts) | Variants/modifiers data model not yet scheduled — needs a migration + Lead sign-off when prioritized |
| **D. Kitchen / KDS** | ✅ | KDS board, Mark ready/Serve ✅ ("Fire" is now rarely seen — see Backend) | Ticket creation on every order, station grouping, item transitions ✅. New tickets fire straight to `preparing` instead of `queued` ✅. A fully-served dine-in ticket flips its table to `served` (calls `floor.ts`'s shared transition primitive directly, best-effort — see `kitchen.ts`'s `patchItem`); a manager can also trigger it by hand from the Floor screen's "Mark served" button, a cashier terminal cannot. Serving an item now also decrements its recipe's ingredient stock via `stock_movements` (`consumeRecipeIngredients`) — allowed to go negative rather than blocking the serve, matching `pos_stock`'s existing oversell reasoning, surfaced via a new "Out of stock" tag on the Inventory screen | `kitchen_tickets`, `kitchen_ticket_items` ✅ | orders.test.ts + kitchen-ticket-status.test.ts + floor.test.ts (table-sync paths) + kitchen.test.ts (5 new PGlite tests: consumption math, negative-stock behavior, idempotency, unit-mismatch skip, no-recipe no-op) ✅. The auto-fire change itself has no new automated test — `push()` is DB+auth-coupled with no existing integration harness, verified manually instead (flagged honestly, not hidden) | Delay/SLA tracking and course-based firing not built. Standalone "Ticket CRUD" deliberately not built — a ticket is derived 1:1 from a paid order; that invariant matters more than the feature |
| **E. Recipes** | ✅ | Recipe builder UI in the product editor (add-dish drawer + a drawer for existing dishes), costed ingredient lines, estimated food-cost % ✅ | `/catalog` recipe + unit endpoints, full replace-on-save ✅ | `units`, `recipes` ✅ | 7 domain tests (costing math), 4 API tests, 4 web tests ✅ | Unit conversion not supported yet — a line only costs when its unit matches the ingredient's stored unit exactly; a mismatch is flagged, not guessed at |
| **F. Restaurant Inventory** | 🟡 | Finished-item stock/oversold reporting ♻️ reusable as-is. Ingredient-level: full `/inventory` screen (list, low-stock + out-of-stock tags, batches with expiry tones, wastage form, movement ledger) ✅, plus cashier-terminal writes gated behind manager-PIN approval ✅. Edit/deactivate an ingredient from the UI: 🔴 (API supports it, nothing calls it yet) | `pos_stock` (finished items) ✅; `ingredients`/`ingredient_batches`/`stock_movements` CRUD + wastage + consumption-wiring ✅ | `pos_stock` ✅; `ingredients`, `ingredient_batches`, `stock_movements`, `recipe_ingredients` ✅ | loadOversold covered; inventory.test.ts (API, wastage-boundary) + inventory.test.ts (web, low-stock/out-of-stock/expiry-tone) + kitchen.test.ts (consumption wiring) ✅ | Two known non-blocking gaps: `ingredients`/`stock_movements.manager_id` uses a bare FK instead of this codebase's composite `(store_id, id)` tenant-scoping convention; no edit/deactivate control in the UI yet |
| **G. Purchasing & Vendors** | 🔴 | none | none | none | none | Beyond the 5-day sprint per original scope note — do not start until Days 3–5 core modules land |
| **H. Customers / CRM** | 🟡 | Guest directory, search (phone or name, one field) ✅, create ✅. Guest profile dialog ("View profile") shows lifetime spend + recent visits ✅, aggregated live from `pos_orders` (no duplicate running-total column). Points balance + tier shown via `<LoyaltyBalance>` on every guest card (directory and the register's guest picker), with an explicit "Enroll in loyalty" action for guests who aren't enrolled ✅. Favorites: 🔴 | Customer CRUD, search (phone or name) ✅. `GET /customers/:id/summary` (+ `/pos/customers/:id/summary`) aggregation endpoint ✅ | `pos_customers` ✅ | customer-api, customer-migration, `customers.test.ts` (lifetime-spend/visit-count aggregation, store+guest scoping, zero-orders case) ✅ | Favorites not built. The tier/balance display now goes through Ahmed's `/loyalty` API (row I) end to end, not a direct-Supabase read — the two independently-built implementations were consolidated onto his during the Day 4 merge |
| **I. Loyalty** | ✅ | Points balance + tier shown on every guest card (Guests screen + register guest picker) via `<LoyaltyBalance>`, with explicit opt-in enrollment, sync/offline-aware states, and a Retry action ✅. Reward rules (create/edit/deactivate/reactivate) manageable from the Guests screen, owner/manager only ✅. A cart line's discount popover now has a "Reward" tab (register, both web and terminal) that lists rewards the guest's balance can afford and applies one as an ordinary line discount ✅. No tier-configuration UI yet — tiers can only be read, not created, so every account shows "No tier" until one is inserted by hand | `/loyalty` (web) and `/pos/loyalty` (terminal): accounts, opt-in enrollment, paginated ledger, tiers (read), reward-rules CRUD ✅, all tenant-scoped. **Checkout wiring ✅**: `orders.ts`'s `push()` awards points (tier-multiplied, based on lifetime points before the order) and deducts a redeemed reward's cost, both in the same transaction as the order itself — idempotent via the existing `operation_id` replay guard, clamped (never sale-blocking) if a queued offline sale's redemption balance turns out stale by the time it syncs | `loyalty_tiers`, `loyalty_accounts`, `loyalty_point_ledger`, `reward_rules` ✅ (Hamza's Day 4 schema, applied and recorded in `APPLIED.md`) | 4 schema-integrity tests + 9 domain tests (`loyalty.test.ts`) + full API test suite (every endpoint, web and terminal paths, cross-store/cross-role rejection) + a web form-draft test + `orders-loyalty.test.ts` (9 subtests: base-rate/tier-rate earning, redeem-and-earn together, clamped redemption, idempotent retry, redemption without a customer rejected) | Nobody can configure a tier yet (needs a small follow-up, same CRUD pattern as reward rules already has) |
| **J. Promotions** | ✅ | Owner/manager-only `/promotions` screen ✅ (create/edit/deactivate, `starts_at`/`ends_at` window, active toggle), routed and in the Manage nav. A cart line's discount popover now has a "Promo" tab listing currently-active promotions and applying one as an ordinary line discount ✅ | `promotionsRouter` (`GET`/`POST`/`PATCH /promotions`), manager-only, in `apps/api/src/app.ts` ✅. **New**: `GET /pos/promotions` (terminal-only, read-only, active-and-in-window filtered) — a gap the checkout-wiring task found: the management endpoints are manager-only by design, but nothing let a cashier terminal *read* promotions to apply one until this | `public.promotions` ✅ (`202609250003_promotions.sql`, applied and recorded in `APPLIED.md`) — `discount_kind`/`discount_value` mirror `LineDiscount`'s `{percent,bps}\|{fixed,cents}` shape for a direct mapping | `promotions.test.ts` (domain: window/active filtering, discount mapping, 8 tests) + `promotions.test.ts` (API/PGlite: constraint checks, active toggle) + `orders-loyalty.test.ts`'s `/pos/promotions` subtest (active/in-window filter, store isolation) ✅ | Applying a promotion doesn't currently check it isn't double-stacked with a reward or a manual discount on the same line — a line still only carries one `LineDiscount`, so this is a UI ordering question (whichever's applied last wins), not a data-integrity gap |
| **K. Staff** | 🟡 | Owner/Manager (web) + Cashier/Manager (terminal) roles ✅, employee management (Settings→Team) ✅. Waiter/Chef/Inventory-manager/Rider as distinct roles: 🔴. Clock-in/shifts/tips: 🔴 | terminal-auth (PIN, sessions, approval) ✅ | `store_memberships`, `terminal_employees` ✅ | terminal-auth security tests ✅ (strong — salted PIN hashing, rollback/expiry) | **Day 4 staff-role review (Hamza) — decision: no distinct "waiter" role this sprint.** `terminal_employees.role` only has `cashier`/`manager` at the database level (`202609150001_terminal_employee_access.sql`'s check constraint) — "waiter" was never a login/permission role, only a floor-assignment relationship (`restaurant_tables.assigned_waiter_id`, validated in `floor.ts`'s `validateWaiter` against *any* active employee, no role filter). Checked whether Day 4's loyalty feature changes that calculus: it doesn't — redemption authority is gated by the existing `manager` role (PIN approval, same as inventory writes), and "who processed a loyalty change" is tracked via `loyalty_point_ledger.created_by_employee_id`/`order_id`, neither of which needs a waiter-specific role to be meaningful. Adding one now would be a role with no actual consumer — revisit only if a real feature needs to distinguish waiters from other cashiers (e.g. tip pooling, waiter-specific commission). Shifts/clock-in still not scheduled this sprint |
| **L. Reports** | 🟡 | Owner/cashier dashboards, daily summary, order history, oversold ✅. Dish profitability/food-cost/kitchen-performance/wastage/loyalty reports: 🔴 | loadDailySummary/loadOrdersPage/loadOversold ✅, timezone-correct | reads existing tables | reports.test.ts (DST-aware, PGlite-backed) ✅ | Day 5 — new reports layer on top of Days 3–4's data. Kitchen-performance data already exists (`kitchen_tickets` timestamps), just unsurfaced |
| **M. Restaurant Intelligence** | 🔴 | none | none | none | none | Correctly not started — no real operational data volume yet to forecast from. Revisit after Day 5 |

## Cross-cutting infrastructure (not a "module" but load-bearing)

| Area | Status | Note |
|---|---|---|
| Auth | ✅ | Two systems by design: Supabase Auth (owner/manager, web) + PIN-based terminal auth (cashier, offline-capable). Don't merge them. |
| RLS / tenant isolation | ✅ | Consistent member-read pattern across every restaurant table added so far; writes go through the API (service-role) only. |
| Offline/sync | ✅ | Dexie outbox pattern, well-tested (order-sync-core, duplicate-submit). Don't introduce a second sync mechanism for new modules — extend this one. |
| Design system | ✅ | MISE tokens (`styles.css` Section E), applied everywhere. See `docs/DESIGN_SYSTEM.md`. |
| CI | ✅ | GitHub Actions, build+test on every PR/push to `develop`/`main`. |
| Testing | 🟡 | Strong unit/integration coverage on money math, checkout, sync, terminal-auth. No component-level or E2E/browser test runs in CI (browser-check scripts exist but are manual-only). |
| Performance | ⚠️ | Single JS bundle is 800KB+ (Vite's own build warning) — no code-splitting beyond three lazy-loaded routes. Flagged for Day 5 hardening, not urgent before then. |
| Migration ledger discipline | 🟠 | `APPLIED.md` process works but has already needed one Lead fixup (Day 2) for a missed entry and a numbering collision between two same-day migrations. `RULES.md` tightens this. |
